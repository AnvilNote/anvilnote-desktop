# AnvilNote Desktop

This repo packages AnvilNote Web, API, Renderer, bundled Typst, fonts, and
installer resources into a desktop application.

This is **not** a monorepo. It expects sibling repos by default:

```
../anvilnote-web
../anvilnote-api
../anvilnote-renderer
```
